// /join/istanbul/<token> — the readable, top-level form of the share link.
// Same page as /planner/join/<token>; the slug is decoration.
export { default, generateMetadata } from "@/app/planner/join/[...parts]/page";
